**RUN**

from main directory:

```
  nvm use
  npm start
```

from server directory:

```
  node server.js
```

**RUN TESTS**

from main directory:

```
  nvm use
  npm run test
```

**REMOVE FROM GENERATED .JS**

from main directory:

```
  nvm use
  npm run clean
```
